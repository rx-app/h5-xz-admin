module.exports = {
  // outputDir: __dirname + '/../server/admin',
  // publicPath: process.env.NODE_ENV === 'production'
  //   ? '/admin/'
  //   : '/'
  // outputDir: __dirname +"/dist",

  baseUrl: "./",   //    这里需要加这个配置   如果文件放在域名 的一个子目录下，需要用相对路径
}